1. * using a docker-compose we can manage a multiple containers in single docker-compose file.
   * we can make communication every container through networks.{bridge network, etc}

## In this scenario we host an python application on docker container in different port and we configured reverse proxy in 
   port 80 using Nginx, with docker file and docker-compose.

